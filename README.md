# DataProcessing API

One-to-one HTTP API adapter for `module-automation-data-processing`. Routes are versioned under `/api/v1/automation/data-processing`, require authenticated team context, and delegate domain mutations to the core package.

